{
    "name": "CHANGEME",
    "license": "UNLICENSE",
    "version": "0.0.1",
    "scripts": {
        "build": "tsup src/*.ts --format cjs,esm --dts --no-splitting",
        "clean": "rm -rf ./dist",
        "lint": "tsc"
    },
    "files": [
        "./dist"
    ],
    "main": "dist/index.js",
    "module": "dist/index.mjs",
    "types": "dist/index.d.ts",
    "devDependencies": {
        "tsup": "^8.0.1",
        "typescript": "^5.3.3"
    }
}
